name: server
description: Server template for dart based client server architecture
version: 1.0.0
publish_to: 'none'
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.3

# Add regular dependencies here.
dependencies:
  grpc: ^3.2.4
  path: ^1.8.0
  protobuf: ^3.1.0
  api:
    path: ../api
  get_it: ^7.6.4
  injectable: ^2.3.2
  logging: ^1.2.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  build_runner: ^2.4.6
  injectable_generator: ^2.4.1
